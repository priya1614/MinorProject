<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/login"
    android:id="@+id/cl3"
    tools:context=".AddCategoryFragment"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:layout_marginTop="100dp"
        android:layout_gravity="center"

        android:foregroundGravity="center"
        >


        <ImageView
            android:layout_marginTop="30dp"
            app:layout_constraintTop_toBottomOf="@+id/ac_title"
            app:layout_constraintLeft_toLeftOf="@+id/ac_title"
            android:id="@+id/ac_image2"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_gravity="center"
            android:foregroundGravity="center"
            android:scaleType="fitXY"
            android:src="@mipmap/ic_launcher_round"/>
        <Button
            android:layout_marginTop="30dp"
            android:layout_gravity="center"

            android:id="@+id/ac_addimage2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/select_category_image"
            app:layout_constraintTop_toBottomOf="@+id/ac_image2"
            app:layout_constraintLeft_toLeftOf="@id/ac_image2">
            
        </Button>
        <Button
            android:layout_gravity="center"
            android:layout_marginTop="30dp"
            android:text="@string/add_category"
            android:id="@+id/add_category_image_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:clickable="true"
            app:srcCompat="@android:drawable/ic_input_add"
            app:layout_constraintTop_toBottomOf="@+id/ac_addimage2"
            app:layout_constraintLeft_toLeftOf="@id/ac_addimage2"/>
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>